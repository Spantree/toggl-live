<div id="selectedNote">
  {{#if model}}
    <h1>name: {{controller.model.name}}</h1>
    {{view Ember.TextArea valueBinding="value"}}
    <button class="btn btn-primary form-control mediumTopPadding" {{action "updateNote"}}></button><br/>
  {{/if}}
</div>

